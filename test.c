// const char *nouns[] = {"cat", "house", "water", "car", "tree"};
// const char *verb[] = {"runs", "jumps", "sits down", "climbs", "talks"};
// const char *adjective[] = {"little", "silly", "wise", "young", "tired"};
// const char *adverb[] = {"quickly", "angrily", "happily"};
// const char *determiner[] = {"the", "a", "my", "your", "his", "her"};
// const char *preposition[] = {"over", "near", "towards", "in front of"};

// int count = sizeof(nouns) / sizeof(nouns[0]);

// srand(time(NULL));            // seed random generator
// int index = rand() % count;   // pick a random index

// printf("Random noun: %s\n", nouns[index]);

#include <stdio.h>

int main() {
    printf("Hello, World!\n");
    return 0;
}

